# LOGIN

To solve this problem look at the page source and open up the JavaScript link for [index.js](index.js).  Format the one line and notice that the username and password is hard coded in the JavaScript.  It is base64 encoded so use CyberChef, or another tool, to decode it and get the answer.